# 更新日志

## 2025-10-27 - 输出格式优化

### 主要变更

1. **移除 IP 地址输出**
   - 文本输出中不再显示 IP 列
   - 图片输出中不再显示 IP 列

2. **新增速度统计**
   - 新增 `平均速度` 列：显示所有下载线程的平均下载速度
   - 新增 `最高速度` 列：显示单个下载线程的最高速度
   - 速度单位：MB/s，保留两位小数
   - 无法连接的节点显示"无法连接"

3. **Emoji 支持优化**
   - 图片渲染使用 `seguiemj.ttf`（Segoe UI Emoji）字体
   - 确保节点名称中的 emoji 能正确显示在图片中
   - 如果系统没有 emoji 字体，自动降级到微软雅黑字体

4. **隐藏 Ping 日志**
   - Ping 过程不再在控制台输出，避免刷屏
   - Ping 功能仍在后台正常工作
   - 最终结果中会显示 Ping 统计数据

### 文件修改

#### `job/test.go`
- 新增 `GetAvgSpeed()` 方法：计算平均下载速度
- 新增 `GetMaxSpeed()` 方法：计算最高下载速度

#### `job/task.go`
- 更新 `RenderText()` 函数：
  - 移除 IP 列
  - 新增平均速度和最高速度列
  - 使用新的 `speedColor()` 函数进行速度着色
- 更新 `RenderImage()` 函数：
  - 移除 IP 列
  - 新增平均速度和最高速度列
  - 使用新的 `getSpeedColorRGBA()` 函数进行速度着色
- 将所有 ping 相关日志从 `Info` 改为 `Debug` 级别

#### `util/image.go`
- 更新字体加载策略：
  - 优先使用 `seguiemj.ttf`（Segoe UI Emoji）字体
  - 降级策略：如果 emoji 字体不可用，使用 `msyh.ttc`（微软雅黑）

### 输出示例

#### 文本输出（启用 Ping）
```
+----------------+-----------+-------------+-------------+----------+----------+----------+
|      NAME      |   TYPE    | AVG-SPEED   | MAX-SPEED   | PING-MIN | PING-MAX | PING-AVG |
+----------------+-----------+-------------+-------------+----------+----------+----------+
| 🇭🇰香港1        | Hysteria2 | 8.45 MB/s   | 12.30 MB/s  | 45ms     | 52ms     | 48ms     |
| 🇺🇸美国2        | AnyTLS    | 无法连接     | 无法连接     | N/A      | N/A      | N/A      |
| 🇯🇵日本1        | Vless     | 15.67 MB/s  | 18.90 MB/s  | 28ms     | 35ms     | 31ms     |
+----------------+-----------+-------------+-------------+----------+----------+----------+
```

#### 文本输出（不启用 Ping）
```
+----------------+-----------+-------------+-------------+
|      NAME      |   TYPE    | AVG-SPEED   | MAX-SPEED   |
+----------------+-----------+-------------+-------------+
| 🇭🇰香港1        | Hysteria2 | 8.45 MB/s   | 12.30 MB/s  |
| 🇺🇸美国2        | AnyTLS    | 无法连接     | 无法连接     |
| 🇯🇵日本1        | Vless     | 15.67 MB/s  | 18.90 MB/s  |
+----------------+-----------+-------------+-------------+
```

### 颜色方案

#### 速度颜色
- 绿色：>= 5 MB/s
- 黄色：2-5 MB/s
- 红色：< 2 MB/s

#### Ping 颜色（毫秒）
- 绿色：< 100ms
- 黄色：100-300ms
- 红色：> 300ms
- 灰色：N/A

### 编译说明

使用提供的 `build.bat` 编译：
```batch
build.bat
```

或手动编译：
```bash
go build -ldflags "-s -w" -o clash-speedtest.exe ./cmd
```

### 使用说明

输出格式没有变化，使用方式保持不变：

```batch
# 文本输出
clash-speedtest.exe --clash-addr "http://127.0.0.1:9097" --clash-secret "your-secret" -n 2 -p

# 图片输出（emoji 完整显示）
clash-speedtest.exe --clash-addr "http://127.0.0.1:9097" --clash-secret "your-secret" -f png -n 2 -p
```

### 注意事项

1. **Emoji 显示**：
   - 图片输出中 emoji 将正确显示
   - 需要 Windows 系统自带的 `seguiemj.ttf` 字体
   - 如果字体文件不存在，会自动降级到微软雅黑

2. **速度计算**：
   - 平均速度 = 总下载量 / 总耗时
   - 最高速度 = 单个线程的最快速度

3. **Ping 日志**：
   - 默认不在控制台显示 ping 过程
   - 如需查看 ping 详情，可以设置日志级别为 DEBUG


